# CLI / Skill Dogfood Log

Date: 2026-06-15

Goal: verify the `find-journalists` skill drives the local `newsjack` REST-wrapper CLI instead of Medialyst MCP tools, and iterate until Opus and Sonnet complete the same workflow set reliably.

## Local Setup

- Built local CLI: `go build -o /tmp/newsjack-dogfood-bin/newsjack ./cmd/newsjack`
- Dogfood PATH prefix: `/tmp/newsjack-dogfood-bin:/Users/elvissun/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin`
- Claude Code binary: `/Users/elvissun/.local/bin/claude`
- Skill supplied to agents: `/Users/elvissun/.agents/skills/find-journalists/SKILL.md`
- Mirror verification: repository skill, `~/.agents`, `~/.openclaw`, and Claude Code runtime `~/.claude` copies have SHA-256 `6be968ef67f1af5ae64ff8c589bc77e7429acd30cf067a074bdd881bf32299e0`.

## Preflight

- `newsjack auth status`: configured, using `~/.newsjack/credentials.json`.
- `newsjack credits balance`: succeeded, 11376.9 available organization credits.
- `newsjack news search --query "AI observability" --tbs qdr:m`: succeeded and returned 10 news results.
- `newsjack media-lists create --name "dogfood disposable REST wrapper 2026-06-15" --empty`: succeeded, created list `chalnb4ncn0jqkeumza3wjvu`.
- `newsjack media-lists inspect chalnb4ncn0jqkeumza3wjvu`: succeeded.
- `newsjack media-lists delete chalnb4ncn0jqkeumza3wjvu`: succeeded.

## Prompt Set

1. Developer-focused AI observability: "Find 8 journalists who'd cover a startup launching a developer-focused AI observability product. Use newsjack."
2. Fintech Series A: "I have a Series A announcement next week for a fintech. Build me a media list."
3. Article-based list: "Take this article URL, build a list around it, and suggest pitch angles." Article URL chosen from live Medialyst news search: `https://thenewstack.io/debugging-observable-ai-systems/`.

## Iterations

| Run | Model | Prompt | Status | Notes / agent transcript summary | Fixes made |
| --- | --- | --- | --- | --- | --- |
| Opus harness check | `claude-opus-4-7` | Prompt set | Failed before agent start | Claude Code exited with `Error: When using --print, --output-format=stream-json requires --verbose`. No workflow ran. | Added `--verbose` to all stream-json dogfood runs. |
| Opus run 1 | `claude-opus-4-7` | Prompt set | Failed / interrupted | Agent read the skill and used only `newsjack` CLI commands. It created media list `tqhxe75i28uhbvomvceo0mvi`, inspected it, hit an action-body error when it sent `column_id` for `run_column`, corrected to `columnId`, then entered long polling for all workflow columns to complete. The local Bash poll did not return control after the agent-requested timeout window, so the run was stopped and the incomplete list was deleted. | Patch skill to say `media-lists action` sends exact API JSON field names such as `columnId`, and to avoid waiting on every enrichment/workflow column before returning a defensible first-wave list. |
| Opus run 2 | `claude-opus-4-7` | Prompt set | Failed / interrupted | Agent again used only `newsjack` CLI commands and created cloud lists, but continued to launch background/polling loops for enrichment completion despite the softer warning. It also tried `newsjack help "journalists enrich"` and got `unknown help topic: journalists enrich` before falling back to `newsjack journalists enrich --help`. Test lists `mu0qww864jczv2l9w85i44ar`, `hhwkf8cxewkszv3cz7cddna2`, `i4vrqxiyunylomy3g3k6n792`, `bj82q8cc4ub8573ewy29tcko`, and `u7qq57kdvq35uivhlti1d5vx` were deleted. | Make skill instruction explicit: do not poll async enrichment jobs/workflow columns in first-wave workflows; use returned job/list IDs as revisit handles. Add CLI help aliases for nested topics like `newsjack help "journalists enrich"`. |
| Opus run 3 | `claude-opus-4-7` | Prompt set | Passed with feedback | Transcript: `eval/dogfood-opus-run3.stream.jsonl`. Agent completed all three prompts with cloud-mode `newsjack` commands only, stopped within the no-poll rule, saved lists `idhc7g5bcptarnh5kko14ibe`, `y9vavj1crjg5yyvrga1fo8y9`, and `yq1vo7g7tjdwkvxbcevglkha`, and clearly marked unresolved bylines/workflow columns as partial cloud mode. It still found that `journalists enrich --help` did not surface `enrich-job`, the skill omitted one-shot job status, and news search needed explicit `publication_type == "editorial"` filtering guidance. | Make REST subcommand `--help` print command-family help with exit 0; add CLI notes for one-shot `enrich-job` and partial async rows; add skill command row for `journalists enrich-job`, editorial filtering, no share links from unresolved tables, and a Partial Cloud Mode example. |
| Opus run 4 | `claude-opus-4-7` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-opus-run4.stream.jsonl`. Agent stayed off MCP and finished, but guessed raw action JSON `{"action":"add_articles"}` before correcting to `add_articles_by_urls`, and refused the fintech Series A list instead of creating a non-pitch-ready research shell. It created lists `k9lkz6mwoifnsafh1fo2yetk` and `sut6erq0zmbacvj7w9mmscju`. | Add `newsjack media-lists add-urls` and `add-keywords` wrappers over the REST actions endpoint; improve nested Zod/API error messages; update the skill to prefer wrappers, name exact raw action discriminators, and allow a small `research-needed` shell for under-specified user asks. |
| Opus run 5 | `claude-opus-4-7` | Prompt set | Passed with actionable feedback | Transcript: `eval/dogfood-opus-run5.stream.jsonl`. Agent stayed off MCP, completed all three workflows, created lists `sva1qxny593n2ubfbth5yn2q`, `ocmie7hpwy8toz0a310osv32`, and `wtk48o8w1ucj8st3dywut1o8`, used `media-lists add-urls`, marked unresolved rows `research-needed`, and refused to pad an 8-person ask with weak names. Remaining friction: `journalists enrich --wait` returned `processing` for most jobs, the agent briefly reused a column ID across lists before correcting to column-name lookup, and the skill needed a clearer honest-undercount/revisit pattern. | Make `journalists enrich --wait` perform CLI-bounded polling against `GET /api/v1/journalist-enrichment-jobs/{jobId}` and document `--poll-timeout-ms` / `--poll-interval-ms`; update the skill to say column IDs are list-specific and honest undercount is correct when bylines remain unresolved; mirror both installed skill copies. |
| Opus run 6 | `claude-opus-4-7` | Prompt set | Failed / interrupted with actionable feedback | Transcript: `eval/dogfood-opus-run6.stream.jsonl` (635 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, ran with `mcp_servers: []`, and completed useful REST calls. It created lists `d8z4vd0p2gacyds0ilhgiczz`, `pu48bqh4kuwswi6dupp15yxi`, and `o6qta6d90z55ijrmpfgmktr2`. Failure 1: it tried `sleep 45 && newsjack media-lists inspect ...`, which the harness blocked. Failure 2: after bounded enrich started returning useful results, it escalated into batch-enriching many news-search URLs in parallel and was stopped to avoid unnecessary API usage. | Strengthen the skill: no `sleep`, delayed inspections, scheduled rechecks, or background waits before inspecting; first-wave enrichment is only for the user's source article or one to three high-confidence anchors; no parallel `--wait` enrich calls; do not pipe needed REST JSON through `head`. Add the same small-batch warning to `newsjack help journalists`. |
| Opus run 7 | `claude-opus-4-7` | Prompt set | Failed / interrupted with actionable feedback | Transcript: `eval/dogfood-opus-run7.stream.jsonl`. Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, ran with `mcp_servers: []`, and completed useful REST calls. It created list `eqriwt6fcfnzqm1eed8273ey` for AI observability and `jdk3tij9rc9zehhw3h3swv61` for the fintech research shell. It used the new bounded `journalists enrich --wait` successfully, including resolved contacts for Jagmeet Singh and Mike Vizard, but then treated "one to three" as soft guidance and ran a fourth enrich before saying it would stop. It also kept truncating enrichment JSON with `head`, then started another long enrich for the article-based prompt and was stopped. | Make the skill's enrichment limit a hard cap of three total `--wait` calls per workflow, including weak or failed calls; tell agents to store full JSON in a temp file and parse it with `python3` or `jq` instead of piping `journalists enrich`, `media-lists inspect`, or `media-lists get` through `head`; mirror both installed skill copies. |
| Opus run 8 | `claude-opus-4-7` | Prompt set | Failed / interrupted with actionable feedback | Transcript: `eval/dogfood-opus-run8.stream.jsonl`. Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, ran with `mcp_servers: []`, read the updated skill, created W1 list `fm9q5szi491o1riuyw3em0nq`, inspected it immediately, and improved JSON handling by saving REST payloads to temp files before parsing with Python. It respected the enrichment counter labels (`call 1/3`, `call 2/3`). Failure: the second `journalists enrich --wait` call ran long enough for Claude Code to background it, creating the same "background wait" operational problem through the CLI default rather than an explicit sleep loop. | Reduce `journalists enrich` default CLI polling from 120s to 45s; make the initial enrich HTTP client timeout match the API `timeout_ms` plus 15s instead of a fixed 2 minutes; update CLI help and the skill examples to use `--poll-timeout-ms 45000`; mirror both installed skill copies. |
| Opus run 9 | `claude-opus-4-7` | Prompt set | Failed / interrupted with actionable feedback | Transcript: `eval/dogfood-opus-run9.stream.jsonl`. Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, ran with `mcp_servers: []`, then invoked Claude Code's `Skill` loader, which pulled a stale `~/.claude/skills/find-journalists` copy that still referenced Medialyst MCP tools and `newsjack mcp-bridge`. It stayed mostly on CLI because the harness also provided current help, but then violated the no-wait rule with `sleep 20 && newsjack media-lists inspect ...`; the run was killed. | Mirror the updated find-journalists skill into Claude Code's runtime `~/.claude/skills/find-journalists/SKILL.md` in addition to the required `~/.agents` and `~/.openclaw` mirrors; verify all four copies share SHA-256 `1081b91e5383dd3e7a3674d1e88d2ad3ccc24e697e1f7f2531902a4790513860`. |
| Opus run 10 | `claude-opus-4-7` | Prompt set | Passed with actionable feedback | Transcript: `eval/dogfood-opus-run10.stream.jsonl` (781 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, ran with `mcp_servers: []`, loaded the fixed Claude Code runtime skill, respected the three-call enrich cap, used no MCP, no sleeps, no shell polling loops, and no share links from unresolved tables. It created lists `df4enxekuom497jfni6ikpl3`, `j0262dk0dpdmstpm4crc6fyr`, and `hav1wxab84wzfdo6y2au5vno`. Enrich jobs were `jej_hztvgr0fwr0dqajrqdpnlhil` (`processing`), `jej_dqvdy4utfxfemt6kgm9s05n5` (completed with Ben Weiss / Fortune), and `jej_g1vc6oa0u5cb6nb32vmykf9z` (`processing`). It returned honest partials and useful pitch angles. Remaining friction: two shell parse errors from complex inline Python, and it first guessed wrong REST response shapes (`articles[]` / top-level fields and flat create response) before inspecting keys and correcting. | Add known REST response shapes for `news search`, `media-lists create`, `media-lists inspect`/`get`, and `journalists enrich`; add safer parser guidance that recommends temp parser files or here-docs over long double-quoted `python3 -c` snippets; mirror all skill copies and verify SHA-256 `ed7cf3e779d8a4e99760471d7b7faa8423c8eaf584d953382373944522cb1d76`. |
| Opus run 11 | `claude-opus-4-7` | Prompt set | Passed with actionable feedback | Transcript: `eval/dogfood-opus-run11.stream.jsonl` (649 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the current skill, stayed off MCP, created lists `v1c7dh5c4jdhq66rm8zsk5l9`, `manzuytv6281wlx83em9rxxo`, and `cjipj7tqg9yu74c5rpefyjem`, returned honest partials, used a temp parser instead of fragile inline Python, and avoided sleeps/background waits. It still had one parser rewrite because `media-lists get` returned `rows` at top level while columns stayed under `media_list`, and it followed a processing enrich result with one immediate `enrich-job` status check. This is useful but not counted as a clean pass after the skill changed. | Update the skill to look for rows both top-level and under `media_list.rows`, to treat null article authors as `research-needed`, and to forbid an immediate `enrich-job` check after a processing `journalists enrich --wait`; mirror all skill copies and verify SHA-256 `e12ed64e399c8fa78a9a402131f344fe74de0dbb307dcd1f2526b7621c8af76f`. |
| Opus run 12 | `claude-opus-4-7` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-opus-run12.stream.jsonl` (567 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the skill with no MCP markers, used only `newsjack` CLI calls and local parsing, created lists `q7g2xdw91bwbuiubrc270jpo`, `x2qwn0fvkn7dt3h10ll6xysw`, and `grzelwjhhrozgbtpaci9u2p5`, and kept unresolved enrich jobs `jej_ynftg72s2mhuajlgy5cw8cvw` and `jej_sck3n4lo097f3nl7j1anwnux` as revisit handles. It used 2/3 enrich calls, no sleeps, no background waits, no polling loops, no immediate `enrich-job`, no parse errors, and no share links from unresolved tables. It returned honest undercounts and pitch angles for the article workflow. Its only notes were API/data-quality limitations: imported rows had `author: null`, enrich jobs were still `processing`, and search results had brand-content noise. | No code or skill changes. Counted as Opus clean pass 1. |
| Opus run 13 | `claude-opus-4-7` | Prompt set | Clean pass 2/2 | Transcript: `eval/dogfood-opus-run13.stream.jsonl` (895 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, used no MCP, stayed within the three-enrich cap, avoided sleeps/background waits/polling loops, did not immediately re-check processing enrich jobs, and created lists `k6ufepur0khokxpwiy13fd6u`, `ac7asz06q08fk8fe7h0y6sag`, and `bpfe9vqfr3dm97g5lcg2osxl`. It resolved Jagmeet Singh / TechCrunch and Rebecca Bellan / TechCrunch through W1 list workflow columns, resolved Ben Weiss / Fortune via the enrich endpoint for W2, and kept The New Stack enrich job `jej_ww6ysmfqn44vu2g1is8k51zx` as a revisit handle. It returned honest undercounts, cuts, pitch angles, and no unresolved share links. Notes were limited to API/data-quality suggestions: broad news search produced brand-content noise, and The New Stack enrich stayed `processing` at 45s. | No code or skill changes. Counted as Opus clean pass 2; move to Sonnet. |
| Sonnet run 1 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run1.stream.jsonl` (699 JSONL lines). Agent stayed off MCP, created one media list `fhrmcks8miru5r19mu2dhr7d`, respected the three-enrich cap, and returned an honest partial when jobs `jej_m2r3ugt355qca6z3edgvkahm` and `jej_xn6wmffr45jt7uj285qhqd3u` were still processing. It failed the run because it only completed workflow 1 instead of all three requested workflows, treated `news search --json` as an output-format flag before correcting, and used `head -80` on a Newsjack JSON payload. | Add a targeted CLI error for `news search --json` explaining that output is JSON by default and `--json` expects an exact request body; add a focused test. Tighten the skill: REST commands print JSON by default, never truncate Newsjack JSON with `head`/`tail`, and when multiple workflows are requested, complete each one and budget enrich calls across the turn. Mirror all skill copies and verify SHA-256 `6f15e0c1305ec1436f910251840518e4ed6f95382a223ffe5ca33540adfc9c45`. |
| Sonnet run 2 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run2.stream.jsonl` (393 JSONL lines). Agent stayed off MCP, avoided the old `news search --json` mistake, did not truncate Newsjack JSON, and avoided sleeps/background waits. It created W1 list `dntcltz6t7o18amphp1eve7g` and enrich job `jej_uyopdmivvu1zllfx0o2cjb9k`, but misread the enrich response by looking for a nested `journalist_enrichment_job` object instead of top-level `id` / `status` / `result`. It also returned a final answer after workflow 1 despite saying it was proceeding to workflow 2. | Clarify the skill's enrich response shape: the payload is the job object itself, with top-level `id`, `status`, `progress`, and `result`. Strengthen the multi-workflow rule so agents must complete every requested workflow before the final answer and must not final after workflow 1 when more workflows remain. Mirror all skill copies and verify SHA-256 `1164ef3574a8448073043837212cc040e673070e8d58fe7db4f70bb2336d06f0`. |
| Sonnet run 3 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run3.stream.jsonl` (858 JSONL lines). Agent completed all three workflows, stayed off MCP, used three total enrich calls, avoided sleeps/background waits, and created lists `j77po983u8d9w2y010j30tme`, `x5bqr2gyrisscpkedf36pv7a`, and `dk364ob5lywr045wkpslhmmy`. It resolved Ben Weiss / Fortune via job `jej_sgaong8soju6reph3dvh3gx0` and kept The New Stack job `jej_kyr7wxni8tf1vdjbg2hhxwx7` as processing. Not clean: it used `python3 ... | head -100` while inspecting enrich JSON, tried a fragile inline Python parser after the skill warned against it, and had to rediscover that enrich status is `complete`, `outlet` is a string, and shared bylines such as `@SiliconANGLE` are weak/non-pitch-ready. | Tighten skill response-shape docs: terminal enrich status is usually `complete`; `result.research` is usually an array; journalist `outlet` is usually a string; publication accounts/shared bylines should be `research-needed`. Strengthen parser guidance: never use `head`/`tail` even for debugging raw structures, print keys/counts/types instead, and use temp parser files for nested JSON instead of quick one-line Python. Mirror all skill copies and verify SHA-256 `dbd92af1928c2b35a72ac835008e7c886e7571dab8fcb1f3c4ccfd5fe2631ef6`. |
| Sonnet run 4 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run4.stream.jsonl` (937 JSONL lines). Agent completed all three workflows, used only CLI/shell parsing, stayed off MCP, avoided `head`/`tail`, avoided sleeps/background waits, stayed within three enrich calls, and created lists `bp8il9x321k97qqvidx52tv7`, `nvnvbig0f6ud360a85jp05q3`, and `a667zys577lyi6pwlgwwvh4j`. It returned an honest partial with Ben Weiss / Fortune from enrich job `jej_vrt5vnh02wj8pswa5i11q3yk`, kept job `jej_ewaxoub2pg2szsg9lzq5jx2f` as processing, and did not pad weak rows. Not clean: it tried unsupported `news search --limit`, hit a parser `KeyError` by assuming inspect rows have `id`, and briefly reused a temp parser that hardcoded the workflow 1 input file when parsing workflow 2. | Add `news search --limit` as a convenience flag mapped to API field `num`, with a focused test and help update. Tighten skill parser guidance: temp parsers should accept `sys.argv[1]` or stdin instead of hardcoding workflow-specific filenames, and inspect rows may have `id`, `row_id`, or no identifier so agents must use a checked fallback. Mirror all skill copies and verify SHA-256 `968daa6631da45acaec5f83bf52a8e3dea9ff9007dd7a22bde5b5515e01741af`. |
| Sonnet run 5 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run5.stream.jsonl` (1048 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the current CLI-mode skill, completed all three workflows, used only `newsjack` CLI calls and local parsing, stayed off MCP, and created lists `p6m38s7bfqtqu7l32x0ia6h1`, `axr8ct668lnylbjyfozuy0q5`, and `v3waupmtyl7gyc2ue513gfj9`. It used exactly three `journalists enrich --wait` calls, avoided `head`/`tail`, avoided sleeps/background waits/polling loops, did not call `enrich-job`, had no command or parser errors, and returned honest partials with pitch angles. Enrich jobs: `jej_svkuqyplqxgqmy9mcd3jcqd4` (processing), `jej_z0ojgjnmk38wmqai3y3k3d8a` (complete; Ben Weiss / Fortune), and `jej_ptrch3bwpglpyyqona3u0rwv` (processing). Minor note: it used `cat` to inspect a small add-keywords metadata response, but did not dump a raw media list or truncate JSON. | No code or skill changes. Counted as Sonnet clean pass 1. |
| Sonnet run 6 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run6.stream.jsonl` (254 JSONL lines, stopped early). Agent verified the CLI-mode skill and created W1 list `t0ftlejaz0o2utilpn95fgwb`, but then drifted into `media-lists action` workflow-column forcing (`run_workflow` / `run_column`) to populate Journalist Profile, used `head -50` while inspecting schema JSON, and ran `sleep 8 && newsjack media-lists inspect ...`. The run was terminated before it spent more API time. | Tighten the skill again: table actions are management actions, not first-wave discovery; do not use `media-lists action` to force workflow columns or async table enrichment during first-wave discovery; do not pass `run_initial_enrichment` for ordinary discovery; do not run `head`/`tail` at all; do not `cat` full Newsjack JSON just to inspect it; prefer temp parser files over `python3 -c`. Mirror all skill copies and verify SHA-256 `cfbc98733d3e3b55ae5c7a5e83860bbed294184f582f2c3752dec61bd0adbf8c`. |
| Sonnet run 7 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run7.stream.jsonl` (325 JSONL lines, stopped early). Agent stayed off MCP and avoided `media-lists action` workflow forcing, but printed full `media-lists inspect` JSON directly, then reverse-engineered row shape with `cat /tmp/wf1_list.json \| python3 -c ...` and `\| head -50`. It also started expanding W1 with additional keywords instead of accepting an honest undercount from unresolved rows. Created W1 list `uv3ryir8aq74chjdjpwm158b` and used two enrich calls before the run was stopped. | Tighten the skill around JSON and URL handling: redirect inspect/get to temp files in command examples; do not use `python3 -c`; do not pipe Newsjack JSON through any command chain; keep selected article URLs in a temp URL file; do not call `media-lists get --include-rows --include-schema` in new first-wave workflows just to recover URLs; do not keep keyword-expanding to hit the requested count. Mirror all skill copies and verify SHA-256 `eb0c4861dd12bc362b21799374716e219529ce22e89b441cd8f21f4bbfa09334`. |
| Sonnet run 8 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run8.stream.jsonl` (179 JSONL lines, stopped early). Agent loaded the current skill and kept selected URLs in `/tmp/workflow1-urls.txt`, but still ran `newsjack media-lists inspect ... > /tmp/wf1-inspect.json && cat /tmp/wf1-inspect.json`, then started a multi-URL `journalists enrich --wait --poll-timeout-ms 90000` call. This would have reintroduced long foreground waits and broad batch enrichment. | Add CLI guardrails to `journalists enrich`: foreground `--wait` with convenience flags now rejects multiple `--url` values, `--poll-timeout-ms` above 45000, and API `--timeout-ms` above 30000. Update help and skill language to say one URL per foreground enrich and 45000 ms max. Mirror all skill copies and verify SHA-256 `5d51b0e51197549d389f949558e2e5003659e85e671dbc30fd52a5121540b16a`; rebuild `/tmp/newsjack-dogfood-bin/newsjack`. |
| Sonnet run 9 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run9.stream.jsonl` (694 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the mirrored CLI-mode skill, completed all three workflows, stayed off MCP, created lists `stbibdrjka628vb323u1213i`, `r3ejj81ed9kksnr9h1kbgr3z`, and `p9gg4z74yh5fk3a2n0jcxhy0`, and used exactly three foreground `journalists enrich --wait --poll-timeout-ms 45000` commands. It had no command errors, no sleeps/background waits/polling loops, no immediate `enrich-job`, no workflow-column actions, no broad multi-URL foreground enrich, and no `head`/`tail`/`python3 -c`/Newsjack-JSON pipe shell patterns. It resolved Ben Weiss / Fortune from job `jej_u06oa334ja08h7hljhykfyh8`, correctly rejected `@SiliconANGLE` as a non-person outlet handle from job `jej_veioifr7bmk8iasyvo58tcju`, and kept The New Stack job `jej_xtfsv5c0uwr675ox1rs204hm` as processing. Note: it used Claude's `Read` tool to inspect temp JSON files and wrote a local text summary with `cat >`, but these did not affect the CLI workflow. It surfaced a data-product gap: standalone enrich jobs return journalist data, but media-list `Journalist Profile` cells stayed null. | No code or skill changes. Counted as Sonnet clean pass 1 on the current guarded CLI/skill. |
| Sonnet run 10 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run10.stream.jsonl` (644 JSONL lines). Agent completed all three workflows, stayed off MCP, created lists `yo0bt5s4e3ynippgco5xoo2r`, `jdtxzxd53u32tq57irhz1o7v`, and `u3p1pmaqs2qi3rdnauxa5bhp`, and used exactly three bounded foreground enrich calls. It resolved Ben Weiss / Fortune from job `jej_x7b0bnj33i7ydnboeu8x58wd`, rejected `@SiliconANGLE` as a weak outlet handle, and kept The New Stack job `jej_oifudo3tq2sqbh7et4a2jx96` as processing. Not clean: it first used Claude Code's editor `Write` tool for a `/tmp` scratch file and hit a tool error, then parsed `media-lists create` with camelCase `mediaList` instead of snake_case `media_list`, causing a `KeyError` before recovering. | Tighten the skill parser guidance: scratch files should use shell redirection/here-docs in agent tools; `media-lists create` uses exactly `media_list` snake_case, never `mediaList`; parser examples now check top-level keys before indexing. Mirror all skill copies and verify SHA-256 `fecb4a244af787fb5b29c25bad2b3941ffb2a663a7df213b370745764bce3cb6`. |
| Sonnet run 11 | `claude-sonnet-4-5` | Prompt set | Passed with feedback / not counted | Transcript: `eval/dogfood-sonnet-run11.stream.jsonl` (132 JSONL lines). Agent completed all three workflows, stayed off MCP, had no command/parser errors, used no sleeps/background waits/polling loops, used no `enrich-job`, no workflow actions, no broad multi-URL foreground enrich, and kept all three `journalists enrich --wait` calls bounded at 45000 ms. It created lists `nktmub6i5f4nj5i7kfwmw2pj`, `vap7eexmhsb13k0xqcjcx2m3`, and `dlcc2oj9stv7z5o305nktofv`; all three enrich jobs were still processing (`jej_grn3m5qexyimu3kckuekujwp`, `jej_kxq9lfh9nz3ilscuos4fpu2u`, `jej_te9j2feq9gpimy5x5d5oidex`). Not counted as final clean because it first ran `newsjack credits` and got command help before correcting to `credits balance`, and it keyword-expanded the source-article workflow after a pending enrich. | Make `newsjack credits` default to the balance endpoint and update help/tests; tighten the skill so source-article workflows do not add keyword rows after pending source-article enrich merely to make the list larger. Mirror all skill copies and verify SHA-256 `4de7ebb04261c20ffea868d442e34d6c9a615cb699bbe00ddc6dd38ade7365a1`; rebuild `/tmp/newsjack-dogfood-bin/newsjack`. |
| Sonnet run 12 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run12.stream.jsonl` (122 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, used `newsjack credits balance` directly, loaded the CLI-mode skill, completed all three workflows, stayed off MCP, created lists `u30hkursrjeskerm412y3xch`, `rr5tz5gqpbuz06b4tui567zq`, and `rifhn2nxwb3tr0l2mffknft4`, and used exactly three bounded `journalists enrich --wait --poll-timeout-ms 45000` calls. No command/parser errors, no sleeps/background waits/polling loops, no `enrich-job`, no workflow actions, no broad foreground enrich, no `head`/`tail`/`python3 -c`, and no source-article keyword row expansion. It rejected `@SiliconANGLE` as an outlet handle, resolved Ben Weiss / Fortune from job `jej_pnrr2s1obuoe3ppa987lzwqg`, and kept The New Stack job `jej_m9572qn1cbkaughcbjmuxxb2` as processing. | No code or skill changes. Counted as Sonnet clean pass 1 on current build/skill. |
| Sonnet run 13 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run13.stream.jsonl` (134 JSONL lines). Agent completed the workflow set but was not clean. It had parser exit errors from nullable row/enrich fields, called `newsjack journalists enrich-job` in the same first-wave turn after a processing `enrich --wait`, piped parser output through `head`, and tried `curl -s ... \| grep ... \| head` against the source article, producing a 394 KB persisted HTML output. | Tighten the skill: `enrich-job` is later revisit only and must not be called in the same first-wave turn; `head`/`tail` are explicit fail conditions; do not scrape source pages with `curl`/`wget`/`grep` to work around null bylines; parsers must treat Medialyst fields as nullable and a parser exception is not clean. Update CLI journalist help wording to label `enrich-job` as later revisit only. Mirror all skill copies and verify SHA-256 `ed601813d6363cd2ea2ad6bb922ebf88594a4cacf5454a3d785e2513cc05a4a5`; rebuild `/tmp/newsjack-dogfood-bin/newsjack`. |
| Sonnet run 14 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run14.stream.jsonl` (130 JSONL lines). Agent avoided the previous `enrich-job`, `head`, and scraping failures, completed the three workflows, and used bounded enrich calls. Not clean: after the source-article enrich for The New Stack stayed `processing`, it searched related coverage and then mutated the source-article list with `newsjack media-lists add-urls ...`, despite the user asking to build around the single source URL. | Tighten the source-article workflow rule: after a pending source-article enrich, do not run `media-lists add-urls` or `add-keywords` just to make the list larger; related searches may inform pitch angles but must not mutate the source list unless the user asked for a broader topic list. Mirror all skill copies and verify SHA-256 `7f682a6490e5a336b1b03542d4542243cd26c22c1ce6d6036a4118be562ca79d`. |
| Sonnet run 15 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run15.stream.jsonl` (96 JSONL lines). Agent stayed off MCP and avoided source-article list mutation, but it spent all three bounded enrich calls on workflow 1, hit a nullable-field parser error before recovering, and then emitted a final answer after only workflow 1 despite saying it was moving to workflow 2. It did not complete workflows 2 or 3, so the run was not clean. | Tighten the multi-workflow budget rule: the three enrich calls are a per-turn hard cap, three workflows default to one enrich each, a second workflow-1 enrich is forbidden until workflows 2 and 3 have first passes, and a final answer before every requested workflow is answered is a failed run. Add a final workflow checklist and sparse-enrich-result guidance. Mirror all skill copies and verify SHA-256 `c86f372f73a52dd9695d4d2b4fc040303f1914e2f08e3e765bd10e816bd8cc87`. |
| Sonnet run 16 | `claude-sonnet-4-5` | Prompt set | Passed mechanically / not counted | Transcript: `eval/dogfood-sonnet-run16.stream.jsonl` (112 JSONL lines). Agent verified the local binary, loaded the CLI-mode skill with `mcp_servers: []`, completed all three workflows, used exactly one bounded enrich per workflow, avoided sleeps/background waits/polling loops, avoided `enrich-job`, avoided workflow actions, and produced honest partial cloud-mode results. Lists: `xi6mdvnsrzi31os3ybeeobnf`, `zgtd802p8nsurlnzj1n0an93`, `nwnctk4bvkcty9rimln0bn7m`. Jobs: `jej_hgh0ex1pjvvlk5n0x1uwqdf3` (processing), `jej_fuszbu07j4evxstjfcnabypf` (complete; Ben Weiss / Fortune), `jej_ovq5wf9ab3erzd5jp0zw9hjg` (processing). Not counted because the final next step said "manually scrape byline," which is too close to the forbidden scrape workaround even though no scraping command ran. | Tighten the no-scrape guidance so follow-up language says "check the article page or outlet byline manually" or "revisit the enrichment job later" instead of "scrape." Mirror all skill copies and verify SHA-256 `a1a61676c612c9cef383ac224888f88a7e8026706b22d4a5c35efef73036f276`. |
| Sonnet run 17 | `claude-sonnet-4-5` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-sonnet-run17.stream.jsonl` (stopped early). Agent stayed off MCP and progressed through workflow 1 into workflow 2, but after a parser missed a completed enrich response it debugged raw enrichment JSON with `python3 ... \| head -100` and then `\| head -80`. It also revealed that completed `journalists enrich --wait` responses can return a completed enrichment batch with top-level `journalists` / `research`, not only a job wrapper with `result.journalists`. | Update the skill's enrich response-shape docs: `journalists enrich --wait` may return either a job wrapper or a completed enrichment batch; parsers must check both `result.journalists` and top-level `journalists`. Add explicit guidance that parser misses should use a tiny shape parser, never raw JSON dumps through `head` / `tail`. Mirror all skill copies and verify SHA-256 `9d52a5e122eae39efe663e77b50bb0d8a1f60fdfb6be0dc89463d0dcd623a56f`. |
| Sonnet run 18 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run18.stream.jsonl` (97 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill with `mcp_servers: []`, completed all three workflows, used exactly three foreground `journalists enrich --wait --poll-timeout-ms 45000` calls spread one per workflow, and returned honest partial cloud-mode results. Lists: `vc50i0pc92hfd5fv8nxlmyl0`, `jpdx50hhpru3tmb22v40w1iz`, `aixwbklrxbvk1fzcy72o9hgq`. Jobs: `jej_ws43rk1vty4k33nedllzcslq` (processing), `jej_mpik3z6ejaajhy5mnoacngl9` (complete; Ben Weiss / Fortune), `jej_fmwf2vkrmj3jpfj8pnjp4x96` (processing). No command/parser errors, no `head`/`tail`, no `sleep`, no `enrich-job` in the first-wave turn, no workflow actions, no source-list mutation, and no invented bylines. | No code or skill changes. Counted as Sonnet clean pass 1 on SHA-256 `9d52a5e122eae39efe663e77b50bb0d8a1f60fdfb6be0dc89463d0dcd623a56f`. |
| Sonnet run 19 | `claude-sonnet-4-5` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-sonnet-run19.stream.jsonl` (stopped early). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill with `mcp_servers: []`, and started workflow 1 cleanly. It created list `gamew46lp6j50b7jvvvugs6k`, inspected it, then hit a parser exception: `AttributeError: 'NoneType' object has no attribute 'get'` while reading a nullable `row.values` cell from `media-lists inspect`. The agent recovered and continued, but parser exceptions are not clean runs. | Tighten `media-lists inspect` / `get` response-shape guidance: individual cells in `row.values` may be `null`; check `isinstance(cell, dict)` before reading `display` or `data`, treat non-dict cells as `research-needed`, and never call `cell.get(...)` directly on a pulled cell. Mirror all skill copies and verify SHA-256 `e033f8c34444d68a3ccaabbc0aae4bd93574dcd3aec75443bb80c7d8ddde04eb`. |
| Sonnet run 20 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run20.stream.jsonl` (114 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill with `mcp_servers: []`, completed all three workflows, used exactly three foreground `journalists enrich --wait --poll-timeout-ms 45000` calls spread one per workflow, and returned honest partial cloud-mode results where bylines stayed unresolved. Lists: `vkb1t1tl1ax522t7evfsfvrl`, `ovgjkihcn6od4jahewdpm5cg`, `gkskxoi34x3s8ymvz6b075yh`. Enrichment resolved Ben Weiss / Fortune for workflow 2 and Oladimeji Sowole / The New Stack for workflow 3, rejected `@SiliconANGLE` as a publication handle for workflow 1, and did not invent contacts for unresolved rows. No command/parser errors, no `head`/`tail`, no `sleep`, no `enrich-job` in the first-wave turn, no workflow actions, no source-list mutation, and no Newsjack JSON truncation. | No code or skill changes. Counted as Sonnet clean pass 1 on SHA-256 `e033f8c34444d68a3ccaabbc0aae4bd93574dcd3aec75443bb80c7d8ddde04eb`. |
| Sonnet run 21 | `claude-sonnet-4-5` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-sonnet-run21.stream.jsonl` (stopped early). Agent verified the local CLI and loaded the CLI-mode skill, completed workflows 1 and 2 behaviorally cleanly, and started workflow 3 with the third bounded enrich call. The third `journalists enrich --wait --poll-timeout-ms 45000` remained live for roughly 70 seconds with an empty output file because the CLI treated `--poll-timeout-ms` as an extra polling window after the initial enrich POST instead of a total foreground budget. | Fix CLI timeout behavior: after the initial `POST /api/v1/journalists/enrich`, subtract elapsed request time before polling the returned job, and do not start a poll GET after the remaining budget expires. Add `TestJournalistsEnrichWaitDoesNotPollAfterForegroundBudget`, update help text to call `--poll-timeout-ms` the total foreground budget, rebuild `/tmp/newsjack-dogfood-bin/newsjack`, update the skill wording, and mirror all skill copies with SHA-256 `b876452794344474a74fdec674db22ab94c1bd7c6ab0758f228b5219e09f3df0`. |
| Sonnet run 22 | `claude-sonnet-4-5` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-sonnet-run22.stream.jsonl` (stopped early). Agent verified the rebuilt local CLI, loaded the CLI-mode skill, created workflow 1 list `x64dvtj6z03x2qti3knl5lny`, and the first enrich respected the new total foreground budget by returning a `processing` job payload (`jej_x50rd88id1cpoh91t6z74gp8`) instead of hanging. Not clean: the first inspect parser treated `row.values` as a positional array and threw `KeyError: 0` before recovering with a second parser. Parser exceptions are failed runs. | Tighten inspect/get response-shape guidance: `row.values` is an object keyed by column ID, not a positional array; check `isinstance(values, dict)`, use `cell = values.get(column_id)`, and never use `values[index]` or infer cell position from column order. Mirror all skill copies and verify SHA-256 `7e4aef6a88b9696c2dea9d5e73ce5a41d6b211947a89eb5f520937c52435b794`. |
| Sonnet run 23 | `claude-sonnet-4-5` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-sonnet-run23.stream.jsonl` (157 JSONL lines). Agent verified the local CLI and loaded the CLI-mode skill, completed all core CLI mechanics cleanly, used exactly three bounded `journalists enrich --wait --poll-timeout-ms 45000` calls, inspected all three created lists, avoided MCP, sleeps, `enrich-job`, workflow actions, source-list mutation, and parser exceptions. Lists: `dyl5jw5df0mahtsz6yxlqk1g`, `xccaxo2e46110ym2dj395myz`, `rgt7ucz1alp52snkzw31ki6u`. Jobs: `jej_x50rd88id1cpoh91t6z74gp8` (processing), workflow 2 resolved Ben Weiss / Fortune, `jej_q1su52320ito2i6qb4iakwdw` (processing). Not clean: it invoked `angle-generator` for workflow 3 and ended with pitch-angle output plus “Now let me compile the full dogfood report” instead of producing the compiled answer across all three workflows. | Tighten final-answer and sub-skill guidance: in multi-workflow `find-journalists` turns, do not hand off to `angle-generator` after list creation; write short angles yourself and end with the full media-list report. Never end with a sub-skill output, pitch-angle-only answer, or a note that you will now compile the report. Mirror all skill copies and verify SHA-256 `a694fc9dc7c8ec2a3eaa5bfe7c6e7d30f321537b8ed9aa04bbec9e99e20ebb2d`. |
| Sonnet run 24 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run24.stream.jsonl` (105 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected all three lists, used exactly three bounded `journalists enrich --wait --poll-timeout-ms 45000` calls, and produced a compiled final report across all workflows. Lists: `sg6mhjyk0fsvfguyb9sy6p0g`, `aha5wqx8gtcacgyewcf8zfii`, `wqtaukyr87jkamc0cw5ayssq`. Jobs: `jej_vtu10wgbspw6w2p96gebh9xb` (processing), `jej_r6vttf1w3nya2qty7pwijyrj` (complete; Ben Weiss / Fortune), `jej_ulvi46hjvzme0yr39odfmsqp` (processing). No command/parser errors, no MCP, no `head`/`tail`, no `sleep`, no `enrich-job` command in the first-wave turn, no workflow actions, no source-list mutation, no angle-generator handoff, and no invented contacts for unresolved rows. | No code or skill changes. Counted as Sonnet clean pass 1 on SHA-256 `a694fc9dc7c8ec2a3eaa5bfe7c6e7d30f321537b8ed9aa04bbec9e99e20ebb2d`. |
| Sonnet run 25 | `claude-sonnet-4-5` | Prompt set | Clean pass 2/2 | Transcript: `eval/dogfood-sonnet-run25.stream.jsonl` (104 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected all three lists, and produced a compiled final report across all workflows. Lists: `nhcdec5a41vec6czl0f7dq9a`, `po68nz1c2p94o55dzfd5xegt`, `qfqxfckme7zivgq2ppu67tel`. Jobs: `jej_plgxe42qb236bjupy5w4mn4i` (processing, reused for workflows 1 and 3 because the source article was the same), `jej_d55sv3wq3q857e6mq998z2uc` (processing). It avoided duplicate source-article enrichment, stayed within the three-call cap with two actual enrich calls, and returned honest partial cloud-mode results. No command/parser errors, no MCP, no `head`/`tail`, no `sleep`, no `enrich-job` command in the first-wave turn, no workflow actions, no source-list mutation, no angle-generator handoff, and no invented contacts for unresolved rows. | No code or skill changes. Counted as Sonnet clean pass 2 on SHA-256 `a694fc9dc7c8ec2a3eaa5bfe7c6e7d30f321537b8ed9aa04bbec9e99e20ebb2d`. |
| Opus run 14 | `claude-opus-4-7` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-opus-run14.stream.jsonl` (stopped early). Agent verified the local CLI and started the workflows, but used Claude Code's editor-style `Write` tool for a scratch file and hit `<tool_use_error>File has not been read yet. Read it first before writing to it.</tool_use_error>` before continuing. Tool errors are not clean dogfood runs. | Tighten scratch-file guidance: for `/tmp` parsers, URL lists, and tiny summaries, use shell redirection or here-docs only; do not use `Write`, `Edit`, or notebook tools because their read-before-write constraints create non-clean tool errors. Mirror all skill copies and verify SHA-256 `9ccebd1b59f8b23fee608659835ada2c000639dd68cd33de9ad9ba247e10ca4e`. |
| Opus run 15 | `claude-opus-4-7` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-opus-run15.stream.jsonl` (stopped early). Agent avoided the prior `Write` tool error and used here-doc parsers, but then used command substitution with `python3 -c` to extract a media-list ID inline: `newsjack media-lists inspect $(python3 -c "...")`. That violates the parser rule and reintroduces fragile one-liner parsing. | Tighten parser guidance: do not use `python3 -c` for command substitution or ID extraction; print IDs with a temp parser file, read the short output, and pass the ID explicitly to the next `newsjack` command. Mirror all skill copies and verify SHA-256 `3f9536f5fbef5d30467e7d2e03e41a4c261be2da914e2f5a6c10da70af5c82b1`. |
| Opus run 16 | `claude-opus-4-7` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-opus-run16.stream.jsonl` (stopped early). Agent loaded the updated skill, used here-doc parsers, and avoided `Write`, but still put `python3 -c` inside a Bash command substitution for a workflow 2 inspect command before correcting with the explicit ID. The presence of the bad command is non-clean. | Add a literal pre-command self-check to the skill: before every Bash command, scan for `python3 -c`, `$(python3 -c`, `head`, `tail`, `sleep`, `curl`, `wget`, `grep`, workflow-column actions, and first-wave `enrich-job`; if present, rewrite before running. Mirror all skill copies and verify SHA-256 `6be968ef67f1af5ae64ff8c589bc77e7429acd30cf067a074bdd881bf32299e0`. |
| Opus run 17 | `claude-opus-4-7` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-opus-run17.stream.jsonl` (118 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected all three lists, used exactly three bounded enrich calls, and produced a compiled final report. Lists: `rfaotyic8jictlwpdihjhnxo`, `lcr2ok7q3ys1kbykr8ipxdt7`, `i7n4cas1f11z9e1sz8hfwtqw`. Jobs: `jej_dk8vol2pqif0nnfwselm6sai` (processing), `jej_ang2hcw7soyl0jovogco7h1s` (complete; Ram Iyer / TechCrunch, soft-fit), `jej_h4u4l410fg2kcwxg6awhh1ku` (processing). No parser/tool errors, no MCP, no `python3 -c` after skill load, no `sleep`, no `enrich-job` command in the first-wave turn, no workflow actions, no source-list mutation, no angle-generator handoff, and no invented contacts for unresolved rows. Note: the initial pre-skill credits check included an unexecuted `head -50` fallback after a successful `newsjack credits balance`; no `head` command ran. | No code or skill changes. Counted as Opus clean pass 1 on SHA-256 `6be968ef67f1af5ae64ff8c589bc77e7429acd30cf067a074bdd881bf32299e0`. |
| Opus run 18 | `claude-opus-4-7` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-opus-run18.stream.jsonl` (31 JSONL lines, stopped early). Agent started cleanly, but tried `newsjack usage`; the CLI printed help then exited non-zero with `unknown command: usage`, creating an avoidable command error before the workflows. | Add top-level `usage` as an alias for `help` with exit 0, add `TestUsageAliasExitsZero`, rebuild `/tmp/newsjack-dogfood-bin/newsjack`, and restart clean-pass counting on the fixed CLI build. |
| Opus run 19 | `claude-opus-4-7` | Prompt set | Failed with actionable feedback | Transcript: `eval/dogfood-opus-run19.stream.jsonl` (85 JSONL lines). Agent completed all three workflows, created lists `ayyqpys4pmtpof0uwjzi75is`, `tnerkky08fmd1c76ovt1hj2w`, and `dbs7o92ybcb79eq69kny5xmd`, used one bounded enrich per workflow, and produced an honest compiled report. Not clean: after loading the skill it ran `newsjack credits balance 2>&1 \| head -50` during setup verification, violating the explicit no-`head` rule. | Tighten the Cloud Mode setup block: show `newsjack credits balance` as a direct setup check and state that it is concise and must not be piped through `head`, `tail`, `cat`, or any other command. Mirror all skill copies and verify SHA-256 `16095a0b5b8c01c27f9ea5a097511ad96ca0a40c86920634864cc1fdc5783bcf`. |
| Opus run 20 | `claude-opus-4-7` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-opus-run20.stream.jsonl` (137 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected lists `d5p9g38cou15do6gydcl200z`, `jc1maupxz1kin6adlh618fjg`, and `acwu2hvcjfhzy6xecqns3p1y`, and produced a compiled final report. Jobs: `jej_iqhovggdacfanv3ercuv4lyr` (processing), `jej_d82nz636l6dyycjrcpirnjw6` (complete; Ben Weiss / Fortune, score 95), and `jej_cvc1etqljbypwl6g5cxhbu03` (processing). No command/parser/tool errors, no MCP, no `head`/`tail`, no `sleep`, no `python3 -c`, no first-wave `enrich-job` command, no workflow actions, no source-list mutation, no sub-skill handoff, and no invented contacts. | No code or skill changes. Counted as Opus clean pass 1 on SHA-256 `16095a0b5b8c01c27f9ea5a097511ad96ca0a40c86920634864cc1fdc5783bcf`. |
| Opus run 21 | `claude-opus-4-7` | Prompt set | Stopped / dogfood-audit feedback | Transcript: `eval/dogfood-opus-run21.stream.jsonl` (partial, stopped early by harness). Agent loaded the CLI-mode skill, ran direct setup checks, created workflow 1 list `ec02qx2r8hjowkxnnqzxi6s9`, and inspected it. The dogfood audit initially flagged `tail` because a parser contained normal words such as `article details`; the command did not invoke the `tail` shell command. The run was stopped before completion, so it is not counted. | Clarify the pre-command rule: `head`/`tail` are forbidden when invoked as shell commands, pipe stages, fallback branches, or command substitutions; ordinary parser words such as `details`, `headline`, or `tailored` are not the issue. Mirror all skill copies and verify SHA-256 `ad7331a72bfba810fe5a1b858bbd089f3331868c71013f3e903f268c8cd3e11a`. Restart clean-pass counting on the clarified skill. |
| Opus run 22 | `claude-opus-4-7` | Prompt set | Failed / stopped with actionable feedback | Transcript: `eval/dogfood-opus-run22.stream.jsonl` (76 JSONL lines, stopped early). Agent loaded the CLI-mode skill, used direct setup checks, but expanded workflow 1 with `newsjack media-lists add-keywords ...` before completing workflows 2 and 3, then inspected that response with `python3 -c`. This reintroduced both count-chasing in a multi-workflow first pass and fragile inline parsing. | Tighten multi-workflow and first-wave rules: do not run `media-lists add-keywords` or `add-urls` to expand workflow 1 before every requested workflow has a first create/inspect/answer pass; treat those wrappers as later expansion tools or explicit existing-list management only. Mirror all skill copies and verify SHA-256 `d1588e1b0a6057016767855f402883b20c65673a777af3531714eb15b3bd9f3d`. |
| Opus run 23 | `claude-opus-4-7` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-opus-run23.stream.jsonl` (103 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected lists `tlchb4j2lu5gsyv8w09lixs7`, `a6f3g3179bvzj48i1thlud17`, and `fb5ef1xh9k6djctr24prqiyj`, and produced a compiled final report. It used exactly three bounded enrich calls: `jej_kk4o7uxs8zbregk0klp2v6e7` (The New Stack, processing), `jej_uz1x2akswzydkzkuo1f9aqgu` (SiliconANGLE, processing), and `jej_io8bebesobj09fdxp2xmufx7` (Fortune, complete; Ben Weiss / Fortune). No command/parser/tool errors, no MCP, no `head`/`tail`, no `sleep`, no `python3 -c`, no first-wave `enrich-job` command, no workflow actions, no source-list mutation, no add-keyword expansion, no sub-skill handoff, and no invented contacts. | No code or skill changes. Counted as Opus clean pass 1 on SHA-256 `d1588e1b0a6057016767855f402883b20c65673a777af3531714eb15b3bd9f3d`. |
| Opus run 24 | `claude-opus-4-7` | Prompt set | Clean pass 2/2 | Transcript: `eval/dogfood-opus-run24.stream.jsonl` (119 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected lists `x34adnlndsj9jz8wju1337o4`, `rx94s1az4cxxbhbbatnx5q4p`, and `tcu3ymfg7nibh3wbdvi10di1`, and produced a compiled final report. It used exactly three bounded enrich calls, one per workflow: `jej_xcuoezqz8t9i2zmhlb14ogcm` (DevOps.com, processing), a completed Fortune enrich resolving Ben Weiss / Fortune, and `jej_jm2jcd0vlrtsf2rvgl12m6w1` (The New Stack, processing). No command/parser/tool errors, no MCP, no `head`/`tail`, no `sleep`, no `python3 -c`, no first-wave `enrich-job` command, no workflow actions, no source-list mutation, no add-keyword expansion, no sub-skill handoff, and no invented contacts. | No code or skill changes. Counted as Opus clean pass 2 on SHA-256 `d1588e1b0a6057016767855f402883b20c65673a777af3531714eb15b3bd9f3d`; move to Sonnet on the same build/skill. |
| Sonnet run 26 | `claude-sonnet-4-5` | Prompt set | Clean pass 1/2 | Transcript: `eval/dogfood-sonnet-run26.stream.jsonl` (122 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected lists `ilro3nv9uh99s8ueh181p40p`, `hyfnilbbywkmfa7pu8ydd75y`, and `j277ay8291iadii2beqdpnvp`, and produced a compiled final report. It used two bounded enrich calls: The New Stack resolved Oladimeji Sowole and was reused for the source-article workflow; Fortune fintech job `jej_pkoyp6t0sul2zzyp2yo2sj4x` remained processing. No command/parser/tool errors, no MCP, no `head`/`tail`, no `sleep`, no `python3 -c`, no first-wave `enrich-job` command, no workflow actions, no source-list mutation, no add-keyword expansion, no sub-skill handoff, and no invented contacts. | No code or skill changes. Counted as Sonnet clean pass 1 on SHA-256 `d1588e1b0a6057016767855f402883b20c65673a777af3531714eb15b3bd9f3d`. |
| Sonnet run 27 | `claude-sonnet-4-5` | Prompt set | Clean pass 2/2 | Transcript: `eval/dogfood-sonnet-run27.stream.jsonl` (119 JSONL lines). Agent verified `/tmp/newsjack-dogfood-bin/newsjack`, loaded the CLI-mode skill, completed all three workflows, created and inspected lists `ucpas7l0v7bohk7m62bp5kxp`, `a310zc48nsnim5smoxkt83h8`, and `kultmrri235ayoq92vec32nv`, and produced a compiled final report. It used three bounded enrich calls: `jej_yc4htw150mhwllx57z6z6cy8` (The New Stack, processing), `jej_tqjgq4541o5vxu7thecwp42g` (Fortune, complete; Ben Weiss / Fortune), and `jej_m0kjkizx2a2ubfz5w89nld6z` (The New Stack source workflow, processing). No command/parser/tool errors, no MCP, no `head`/`tail`, no `sleep`, no `python3 -c`, no first-wave `enrich-job` command, no workflow actions, no source-list mutation, no add-keyword expansion, no sub-skill handoff, and no invented contacts. | No code or skill changes. Counted as Sonnet clean pass 2 on SHA-256 `d1588e1b0a6057016767855f402883b20c65673a777af3531714eb15b3bd9f3d`. Dogfood loop complete: Opus runs 23/24 and Sonnet runs 26/27 are clean on the same CLI build and skill SHA. |
